<% provide(:title, 'All Users') %>
<h1>All Users</h1>
<%= will_paginate %>
<ul class='users'>
	<% @users.each do |u| %>
		<li>
			<%= gravatar_for u, size: 50 %>
			<%= link_to u.name, u %>
		</li>
	<% end %>
</ul>
<%= will_paginate %>
